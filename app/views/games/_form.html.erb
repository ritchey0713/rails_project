<%= form_for [@user, @game] do |f| %>

  <% if @game.errors.any? %>

    <% @game.errors.full_messages.each do |msg| %>
      <%= msg %>
    <% end %>
  <% end %>

  What is the name of this game?
  <%= f.text_field :name %></br>

  What kind of game is this?
  <%= f.text_field :game_type %></br>

  How many hours have you played?
  <%= f.number_field :play_time %></br>

  Who/what is your main character?
  <%= f.text_field :main_setup %></br>

  what is your current skill rating?
  <%= f.number_field :score %>

  <!-- <%#= f.fields_for :rankings do |com| %>
  <%#= com.hidden_field :user_id, :value => @user.id %>

  Leave a comment! : <%#= com.text_field :comment %>
  <#% end %> -->


  <%= f.submit "Click to submit!", :class => "js-submit" %>
<% end %>
